import {getContent} from '@/lib/content';import Link from 'next/link';export default function Page({params}:{params:{audience:string}}){const routes=getContent().routes[params.audience]||[];if(!routes.length)return <div>Unknown audience</div>;return (<div className='rounded-2xl p-6 border border-white/10 bg-white/5'><h1 className='text-2xl font-semibold'>Recommended route: {params.audience}</h1><ol className='list-decimal ml-6 mt-4'>{routes.map((r:string)=>(<li key={r}><Link href={r}>{r}</Link></li>))}</ol></div>)}